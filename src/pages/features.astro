---
import { defaultLang } from "../i18n/ui";

const redirectTo = `/${defaultLang}/features`;
---

<meta http-equiv="refresh" content={`0;url=${redirectTo}`} />
<script>
  window.location.replace(window.location.origin + "${redirectTo}");
</script>
